/**
 * The page to show user that you are offline.
 * This is active only if PWA is enabled for the application.
 * @param {*} props 
 * @returns 
 */
export default function PageOffline(props){
    return(
        <>
         Currently your are offline, once connected internet you would be able to get more updates!!
        </>
    )
};